# Copy to .env and fill in. Required for STT + LLM. TTS falls back to the browser if unset.
OPENAI_API_KEY=

# Optional overrides
OPENAI_STT_MODEL=whisper-1
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_TTS_MODEL=tts-1
OPENAI_TTS_VOICE=alloy
